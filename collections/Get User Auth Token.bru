meta {
  name: Get User Auth Token
  type: http
  seq: 7
}

get {
  url: {{baseURL}}/api_auth?token={{uuid}}&service=apple-notes
  body: none
  auth: none
}

query {
  token: {{uuid}}
  service: apple-notes
}
